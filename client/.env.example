# Base URL of the backend API.
# In dev you can leave this as /api (Vite proxies it to http://localhost:5000).
# In production set the full deployed backend URL, e.g.
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api
